apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "etap-service.fullname" . }}-test-connection"
  labels:
    {{- include "etap-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "etap-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
